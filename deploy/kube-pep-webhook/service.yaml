apiVersion: v1
kind: Service
metadata:
  name: pep-webhook
  namespace: default
  labels:
    app: pep-webhook
spec:
  selector:
    app: pep-webhook
  ports:
  - port: 8443
    targetPort: 8443
    protocol: TCP
    name: https
  type: ClusterIP

